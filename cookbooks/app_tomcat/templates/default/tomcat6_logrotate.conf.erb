/var/log/<%= @tomcat_name %>/*log /var/log/<%= @tomcat_name %>/*.out {
    missingok
    notifempty
    nocompress
    sharedscripts
    postrotate
        /bin/kill -HUP `cat /var/run/syslogd.pid 2> /dev/null` 2> /dev/null || true
    endscript
}